/* Wyatt Geckle
 *
 * MIPS string functions
 */


#include <stdint.h>

/* Computes the length of a null-terminated string. */
int strlen(char* str);

/* Converts a string to an unsigned 32-bit integer. */
uint32_t str_to_u32(char* star);

